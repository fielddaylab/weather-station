using System.Collections;
using BeauUtil;
using FieldDay;
using FieldDay.Systems;
using UnityEngine;

namespace WeatherStation {
    public class VOCode : MonoBehaviour
    {
        [SerializeField] public uint VOLineCode;

        // Start is called before the first frame update
        void Awake()
        {
            
        }
    }
}